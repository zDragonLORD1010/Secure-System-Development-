# Шпаргалка по базовому Python

# Типы данных

`integer` - Целые числа (Например: 0, -1, -34, 123, 43, и т.д)

`float` - Дробные числа (Например: 0.123, 0.1, -12,234, 5/2, и т.д)

`char` - Символы (Например: 'c', 's', '1', '+', и т.д)

`string` - Строки (Например: "Hello World!", "12313", "jdnfjn", "jsn&^@#f723", и т.д)

`boolean` - Логические переменные (Павда (*True*) или ложь (*False*))

# Переменные и арифметические операторы (`+`, `-`, `/`, '*', `=`, `//`, `%`)

Если простыми словами, то переменные это ящики, в которые вы можете положить любое значение. **Переменные можно назвывать как угодно**, главное, чтобы между названием не было пробелов (вместо пробела можно использовать `_`, например `string_123`) и символов по типу ('+', '-', '@', и т.д).

## Присвоение значения (чему равна переменная или, простыми словами, `=`)

Например, я хочу в переменную `a` положить яблоко (🍎), тогда я напишу (Вместо яблока может быть любое значение: -1, 2.67, 'c', ')', "Hello", True, и т.д):

```py
a = 🍎 # Я присваиваю переменной "a" значение 🍎.
```

## Сложение (`+`)

Складывает числа, символы, строки. Главное - обозначить что вы делаете: складываете строки, числа, символы и т.д.

Например, пусть переменная `a` будет равна `10`, а переменная b - `20`, тогда:

```py
a = 10    # 10 - число
b = 20    # 20 - число
c = a + b # Я записываю в переменную сумму a и b, c будет равно 30 (10 + 20)
```

**ВАЖНО!** Если в переменных `a` и `b` будут лежать значение не `10` и `20`, а `'10'` и `'20'` соответственно, то результат сложения будет другой:

```py
a = '10'  # '10' - строка
b = '20'  # '20' - строка
c = a + b # В этом случае в c запишется результат их склеивания, то есть '10' + '20' = '1020' и c будет также строкой
```

## Вычитание (`-`), деление (`/`), умножение (`*`)

Эти три операции можно проводить только с двумя числами (`integer` или `float`), например:

```py
a = 100     # 100 - число
b = 2       # 2 - число

c_1 = a - b # В с_1 разница a на b, то есть c_1 будет равно 98 (100 - 2)
c_2 = a / b # В с_2 запишется результат деления a на b, то есть c_2 будет равно 50 (100 / 2)
c_3 = a * b # В с_3 запишется результат умножения a на b, то есть c_3 будет равно 200 (100 * 2)
```

**НО!** Строку можно умножить на целое число, и тогда она повториться столько раз, сколько вы скажите, например:

```py
a = "Hello" # "Hello" - строка
b = 3       # 3 - число

c = a * b   # В c запишется строка a b раз, то есть с будет равно "HelloHelloHello" ("Hello" * 3)
```
 
## `mod` (`%`) и `div` (`//`)

`mod` - это остаток от деления одного числа на другое

`div` - целая часть от деления одного числа на другое

Например у меня в коробке `box` лежит 10 яблок (🍎🍎🍎🍎🍎🍎🍎🍎🍎🍎) я хочу их разделить между тремя людьми: `Ivan`, `Masha`, `Sasha`. Тогда, кажому я могу дать по 3 яблока: `Ivan` (🍎🍎🍎), `Masha` (🍎🍎🍎), `Sasha` (🍎🍎🍎), а одно яблоко (🍎) у меня останется в корзине, потому что я хочу разделить между ними яблоки поровну. Таким образом, если мы разделим 10 на 3 мы получим: `10 / 3 = 3 (1 осталось)`, где `3` - это результат целочисленного деления `div`, а `1` - результат подсчета остатка `mod` от 10 яблок между 3 людьми. Вот как это работает в `Python`:

```py
box = 10     # 10 - число, сколько яблок лежит в корзине
b = box // 3 # В b я посчитал сколько яблок я могу дать каждому, чтобы было поровну, то есть b равно 3 (10 // 3)
c = box % 3  # В с я посчитал, сколько яблок останется в корзине, то есть c равно 1 (10 % 3)
 ```

# Ввод и вывод простых переменных (`input()` и `print()`)

`input()` - команда, которая позволяет ввести число, строку, символ и т.д.

`print()` - команда, которая выводит что угодно в формате строки (`string`)

`int()` - команда, которая переводит строку в целое число (`integer`), например, из строки ''10'' мы получим число `10`

`float()` - - команда, которая переводит строку в дробное число (`float`), например, из строки `10.123` мы получим число `10.123`

`str()` - команда, которая переводит что угодно в строку (`string`), например из числа `1000.101` мы получим строку `'100.101'`

Давайте разберемся как это работает в коде:

```py
a = input()  # Пусть в а вводится число `10`, тогда Python записывает в переменную a `'10'`
a = int(a)   # Поэтому мы уточняем, что `10` это целое число командой int(), и в новое значение a записывается предыдущее значение, только ЦЕЛОЕ, то есть `a = 10`, а не `a = '10'`

# ИЛИ тоже самое можно записать с помощью

a = int(input())

b = input()  # Пусть в b вводится строка "Hello", тогда Python записывает в переменную a "Hello"

print(a)     # Выводит значение а и переносится на следующую строку. То есть в консоли будет выведено 10

print(a, b)  # Выводит значения а и b через пробел и переносится на следующую строку. То есть в консоли будет выведено 10 Hello
```

**Давайте решим задачу:** Вводится 2 целых числа a и b, нужно посчитать их сумму и вывести `"Сумма = {сумма_a+b}. Ура!"`:

```py
# Предположим, что мы ввели 3 и 2 с разных строк в консоли

a = input()  # Вводим a и b (Пока программа считает их строками)
b = input()

a = int(a)   # Уточняем, что a и b это целые (integer) числа
b = int(b)

summ = a + b # Записываем результат сложения a и b в переменную summ

print("Сумма = " + str(summ) + ". Ура!")

# ИЛИ тоже самое можно вывести с помощью

print(f`Сумма = {summ}. Ура!`)
```

# Логические и условные операторы (`if`, `elif`, `else`)

```py
if (условие_1):
    действие_1
elif (условие_2):
    действие_2
else:
    действие_3
```

`if (условие_1):` - это условие (**если (условие_1), то ...**). Если `условие_1` выполнено, то код исполнит `действие_1`.

`elif (условие_2):` - это такое же условие (**иначе если (условие_1), то ...**) как и `if`, только `elif` проверяет свое `условие_2` только в том случае, если до этого `условие_1` не было выполнено успешно. Тогда, если `условие_2` выполнено, то код исполнит `действие_2`.

`else:` - выполняет `действие_3`, если все условия до этого момента (`условие_1`, `условие_2` и т.д) не были выполнены.

## Некоторые правила использования:

1. При наличии `if` не обязательно использовать `elif` и `else`
2. `else` может быть только 1 раз для кажого **своего** `if`
3. `elif` это дополнительно условие, которое может быть только после `if` и может быть использовано бесконечное число раз
4. `if`, `elif`, `else` (относящиеся друг к другу) должны быть на одной ввертикальной линии в коде.

## Как это работает?

Разберем это на примере кода:

```py
a = input() # Получает любую строку на ввод

if (len(a) != 1): # Если строка состоит больше (или меньше) чем из одного символа, то выводит "Здесь больше одного символа"
    print("Здесь больше одного символа")
else: # Если строка состоит из одного символа, то переходит к следующим проверкам
    if (a.isdigit()): # Если а - цифра, пишет "Цифра"
        print("Цифра")
    elif (a.islower()): # Если a не цифра, то проверяет является ли a маленькой буквой. Если да, то выводит "Маленькая буква"
        print("Маленькая буква")
    elif (a.isupper()): # Если a не цифра и не маленькая буква, то проверяет является ли a большой буквой. Если да, то выводит "Большая буква"
        print("Большая буква")
    else: # Если a не цифра, не маленькая буква и не большая буква, то выводит "Непонятный символ"
        print("Непонятный символ")
```

# Ввод и вывод строк, переменных записанных через пробел

```py
a = input() # Ввод любой строки
a1, a2, a3 = a.split(" ") # Делит строку a на 3 части (если нужно больше или меньше добавляете a4, a5 и т.д, либо убираете) через разделитель записанный в скобках у split (данном случае делит через пробел)

# ИЛИ
a1, a2, a3 = input().split(" ")
```

# Циклы (`for`, `while`)

## Цикл `for`

```py
for i in range(с_какого_элемента_начинаем, ДО_какого_элемена_делаем, шаг):
    действие
```

Цикл `for` выполняет `действие` пока `i` принимает значения от `с_какого_элемента_начинаем` до `ДО_какого_элемена_делаем` с шагом `шаг`.

У цикла `for` есть 3 значения:

1. 'с_какого_элемента_начинаем' показывает с какого `i` цикл начнет работу (если ничего не писать, то по стандарту будет `0`)
2. `ДО_какого_элемена_делаем` показывает до какого значения цикл работает, то есть условие (i < `ДО_какого_элемена_делаем`)
3. `шаг` показывает на какое значение будет увеличиваться `i` (если ничего не писать, то по стандарту будет `1`)

То есть для цикла `for i in range(0, 5, 1)` `i` будет принимать значения от `0` до `4` (до `4` потому что `4 < 5` - условие выполняется, `5 < 5` - условие не выполняется). Этот же цикл можно записать как `for i in range(5)` потому что `0` и `1` всегда стоят в цикле по стандарту.

Разберемся на примере:

```py
a = input() # Вводится строка "Hello"

for i in range(len(a)): # Цикл работает от 0 до 5 (не включая 5) (5 - длинна слова "Hello")

    # Если нужно вывести в столбик
    print(a[i]) # Мы обращаемся к строке a и выводим элемент под номером i (В строках номер буквы всегда будет на 1 меньше, так буква "H" первая, но ее индекс равен 0)
    print(i)    # Выводит все индексы (все значение i, которые происходят в цикле)

    # Если нужно вывести в строку через пробел
    print(a[i], end=" ") # Выводит букву
    print(i, end="")     # Выводит индекс i
```

В этой задаче мы ввели слово `Hello`, дальше идет цикл `for`, в котором `i` принимает значения от `0` до `4`:

1. Сначала `i = 0` цикл начинает работу. Выводит букву "H", потому что в слове `Hello` буква с индексом `i = 0` это "H", затем выводи сам индекс `0`. После этого к `i` прибавляется его шаг, в данном случае `шаг = 1`, то есть `i = i + 1 = 0 + 1 = 1`
2. Проверяется условие `i < len("Hello")`: `1 < 5` - да, значит цикл начинает действие с `i = 1`. Выводит букву "e" и индекс `1`. После этого к `i` прибавляется его шаг, то есть `i = i + 1 = 1 + 1 = 2`
3. Проверяется условие `i < len("Hello")`: `2 < 5` - да, значит цикл начинает действие с `i = 2`. Выводит букву "l" и индекс `2`. После этого к `i` прибавляется его шаг, то есть `i = i + 1 = 2 + 1 = 3`
4. Проверяется условие `i < len("Hello")`: `3 < 5` - да, значит цикл начинает действие с `i = 3`. Выводит букву "l" и индекс `3`. После этого к `i` прибавляется его шаг, то есть `i = i + 1 = 3 + 1 = 4`
5. Проверяется условие `i < len("Hello")`: `4 < 5` - да, значит цикл начинает действие с `i = 4`. Выводит букву "o" и индекс `4`. После этого к `i` прибавляется его шаг, то есть `i = i + 1 = 4 + 1 = 5`
6. Проверяется условие `i < len("Hello")`: `5 < 5` - нет, значит цикл начинает заканчивает свою работу.

## Цикл `while`

```py
while (условие):
    действие
```

Цикл `while` повторно выполняет `действие` **пока** выполняется любое `условие`.

Разберемся на примере:

```py
a = int(input()) # Вводим целое число, представим, что a = 10
b = int(input()) # Вводим целое число, представим, что b = 6

while (a > b): # Пока выполняется условие a > b цикл делает действия
    print("Yes") # Выводит "Yes"
    a = a - 1 # Отнимает от a единицу
```

В этой задаче мы ввели два целых числа (`a = 10` из первой строки ввода, число `b = 6` из второй строки ввода). Дальше начинает работать цикл:

1. Проверяет условие `a > b`: `10 > 6` - да, поэтому цикл начинает выполнять действие: выводит "Yes", потом отнимает от `a` единицу, то есть `a = a - 1 = 10 - 1 = 9`
2. Проверяет условие `a > b`: `9 > 6` - да, поэтому цикл начинает выполнять действие: выводит "Yes", потом отнимает от `a` единицу, то есть `a = a - 1 = 9 - 1 = 8`
3. Проверяет условие `a > b`: `8 > 6` - да, поэтому цикл начинает выполнять действие: выводит "Yes", потом отнимает от `a` единицу, то есть `a = a - 1 = 8 - 1 = 7`
4. Проверяет условие `a > b`: `7 > 6` - да, поэтому цикл начинает выполнять действие: выводит "Yes", потом отнимает от `a` единицу, то есть `a = a - 1 = 7 - 1 = 6`
5. Проверяет условие `a > b`: `6 > 6` - нет, поэтому цикл заканчивает работу

## Команды `break` и `continue`

`break` - останавливает цикл

`continue` - пропускает одно действие в цикле

# Символы и строки

Разберемся к какому элементу/элементам строки мы обращаемся `string` - строка:

- `string[a]` - Символ строки под номером `a`. Например, если строка `string = "Hello"`, а `a = 1`, то `string[a] = 'e'` (потому что у слова `"Hello"` `'H'` имеет индекс `0`, `'e'` имеет индекс `1`, `'l'` имеет индекс `2`, `'l'` имеет индекс `3`, `'o'` имеет индекс `4`)

- `string[-a]` - Символ который по счету `a` с конца. Например, если строка `string = "Hello"`, а `a = 1`, то `string[-a] = 'o'` (последний символ слова `"Hello"`), а если `a = 4`, то `string[-a] = 'e'` (4 символ c конца слова `"Hello"`)

- `string[a:b:c]` - Несколько символов от `a` до `b` с шагом `c` (тоже самое как и при цикле `for`). Например, если строка `string = "Hello"`, а `a = 1`, `b = 3`, `c = 1`, то `string[a:b:c] = "el"`

- `string[0:-a]` -  Несколько символов кроме последних `a` символов. Например, если строка `string = "Hello"`, а `a = 2`, то `string[0:-a] = "Hel"`

- `string[::-1]` - Строка `string` наоборот. Например, если строка `string = "Hello"`, то `string[::-1] = "olleH"`

- `len(string)` - Длина строки. Например, если строка `string = "Hello"`, то`len(string) = 5`

- `string.replace(a, b)` Заменяет в строке `string` все значения `a` на значение `b`. Например, если `string = "abcABCabc"`, `a = "abc"`, `b = "123"`, то `string.replace(a, b) = "123ABC123"`

- `string.count(a)` - Считает количество символов `a` в строке `string`. Например, если `string = "abcABCabc"`, `a = "a"`,  то `string.count(a) = 2`

- `ord(a)` - Переводит любой символ `a` в число (ее номер в таблице **ASCII**). Например, если `a = 'b'`, то `ord(a) = 98`

- `chr(a)` - Переводит число в символ (ее значение в таблице **ASCII** ). Например, если `a = 101`, то `ord(a) = 'e'`

# Одномерные списки (массивы) в Python

Одномерный список это **шкафчик**, в который мы помещаем какие-то **переменные**. Например, если я хочу положить 3 яблока (🍎🍎🍎) в шкаф на разные полки, то на каждой полке у меня будет лежать по яблоку (🍎). Получается в шкафчике у меня будет `[🍎, 🍎, 🍎]`.

С одномерными массивами мы можем работать также как и со строками, только в строках при обращении к символу через `string[i]` мы получим только один символ, а в массиве при обращении `array[i]` мы можем получить, как один символ, так и строку любой длинны.

## Ввод и вывод одномерных списков (массивов)

```py
# Создание массива a заполненного нулями размера n. Например при n = 3, в массиве a будет [0, 0, 0]
a = [0] * n


# Создание массива из строк записанных в одной строке через разделитель (в данном случае - пробел)
a = input().split(" ")


# Если при создании массива из строки все символы являются числами, то переводим их в целые числа ('1' - строка, 1 - число)
# Тогда, если a = ['1', '2', '3'] ('1', '2', '3' - строки), то после цикла a = [1, 2, 3] (1, 2, 3 - цифры)
for i in range(len(a)):
    a[i] = int(a[i])


# Создание массива размером n, если в него вводсятся числа каждый раз с новой строки
n = int(input())
a = [0] * n
for i in range(n):
    a[i] = input()

# ИЛИ Создание массива размером n, если в него вводсятся числа каждый раз с новой строки (другим способом)
n = int(input())
a = []
for i in range(n):
    a.append(input()) # Команда append добавляет введенную переменную в массив а


# Вывод всех элементов массива в столбик, где а - название массива
for i in range(n):
    print(a[i])


# Вывод всех элементов массива в одну строку, где а - название массива
for i in range(n):
    print(a[i], end=" ")
print()

# ИЛИ вывод массива в одну строку (другим способом)
string = ""
for i in range(n):
    string = string + str(b[i]) + " "
print(string)
```

# Двумерные списки (массивы / матрицы) в Python

Двумерный список это **комната**, в который мы помещаем **шкафчик**, в котором лежали какие-то **переменные**. Например, я хочу поставить в комнату 3 шкафа, в которых лежат по три яблока, то мы значем, что в **шкафчике** лежат яблоки `[🍎, 🍎, 🍎]`, тогда в комнате будет `[[🍎, 🍎, 🍎], [🍎, 🍎, 🍎], [🍎, 🍎, 🍎]]`.

С двумерными массивами мы можем работать также как и со строками и одномерными массивами, только в строках при обращении к символу через `string[i]` мы получим только один символ, а в двумерном массиве при обращении `matrix[i][j]` мы можем получить, как один символ, так и строку любой длинны.

## Ввод и вывод двумерных списков (массивов / матриц)
 
```py
# Создание пустого двумерного массива (матрицы) размера N на M
N = int(input()) # Вводим количество столбцов в матрице
M = int(input()) # Вводим количество строк в матрице
matrix = [] # Матрица (Двумерный массив)
for i in range(N):
    row = [] # Строка в матрице (Одномерный массив)
    for j in range(M):
        row.append(0)
    matrix.append(row) # Добавление строки в матрицу

# Создание двумерного массива (матрицы) размера N на M, при вводе каждого элемента с новой строки
N = int(input()) # Вводим количество столбцов в матрице
M = int(input()) # Вводим количество строк в матрице
matrix = [] # Матрица (Двумерный массив)
for i in range(N):
    row = [] # Строка в матрице (Одномерный массив)
    for j in range(M):
        row.append(input()) # Добавление элемента в строку
    matrix.append(row) # Добавление строки в матрицу


# Создание двумерного массива (матрицы) размера N на len(row), при вводе строк с элементами
N = int(input()) # Вводим количество столбцов в матрице
matrix = [] # Матрица (Двумерный массив)
for i in range(N):
    row = input().split(" ") # Вводим строку разделенную разделителем
    matrix.append(row)

# Вывод матрицы (Как таблица)
for i in range(N):
    for j in range(M):
        if j == M-1:
            print(matrix[i][j])
        else:
            print(matrix[i][j], end=" ")
```

# Кортежи в Python

Во многом кортежи похожи на массив или строку только значения кортежа записываются в скобках:

```py
tuple = (значение_1, значение_2...)
```

## Полезные команды

- Почти все команды записанные для строк в разделе этой шпоры **Символы и строки** работают и для кортежей

- `tuple[a]` - Символ кортежа под номером `a`. Например, если кортеж `tuple = (1, 'H', 'Hello')`, а `a = 2`, то `tuple[a] = 'H'`

- `tuple[a:b:c]` - Несколько символов от `a` до `b` с шагом `c` (тоже самое как и при цикле `for` или со строками)

- `len(tuple)` - Длина кортежа `tuple`. Например, если кортеж `tuple = (1, 'H', 'Hello')`, то `len(tuple) = 3`

- `min(tuple)` - Минимальный элемент кортежа `tuple`. Например, если кортеж `tuple = (1, 2, 3)`, то `min(tuple) = 1`

- `max(tuple)` - Максимальный элемент кортежа `tuple`. Например, если кортеж `tuple = (1, 2, 3)`, то `max(tuple) = 3`

- `tuple.count(a)` - Считает количество символов `a` в кортежt `tuple`. Например, если `tuple = (1, 2, 3, 1, 2, 1, 1)`, `a = 1`, то `tuple.count(a) = 4`

- `tuple.index(a)` - Индекс под которым встречается переменная равная `a` в кортеже tuple. Например если `tuple = (1, 2, 3, 'a', 'b', 'c')` и `a = 'a'`, то `tuple.index(a) = 3`

- Создание кортежа:
   - С помощью круглых скобок: `tuple = (1, 2, 3, 'a', 'b', 'c')`
   - Без круглых скобок: `tuple = 1, 2, 3, 'a', 'b', 'c'`. (`tuple = (1, 2, 3, 'a', 'b', 'c')`)
   - Используя встроенную функцию `tuple()`: `tuple = tuple([1, 2, 3, 'a', 'b', 'c'])`. (`tuple = (1, 2, 3, 'a', 'b', 'c')`)
   - Из введенного элемента, например строки, с помощью функции `tuple()`: `tuple = tuple("hello")`. (`tuple = ('h', 'e', 'l', 'l', 'o')`)
   - Можно создать пустой кортеж: `tuple = ()`

- Сложение и умножение кортежей:
```py
# Сложение кортежей (Пример)
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
tuple = tuple1 + tuple2 # tuple = (1, 2, 3, 4, 5, 6)


# Умножение кортежей (Пример)
tuple1 = (1, 2, 3)
tuple = tuple1 * 3 # tuple = (1, 2, 3, 1, 2, 3, 1, 2, 3)


# Пример использования сложения и умножения
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
tuple = tuple1 * 3 + tuple2 * 2
print(tuple) # Выведет (1, 2, 3, 1, 2, 3, 1, 2, 3, 4, 5, 6, 4, 5, 6)
```

# Множества в Python

Множество (`set()`) - это изменяемая структура данных, которая хранит набор **уникальных элементов**.

Множества могут содержать только неизменяемые объекты:

- Целые числа (`integer`)
- Числа с плавающей точкой (`float`)
- Строки (`string`)
- Кортежи (`tuple`)

## Создание множеств

### C помощью фигурных скобок

```py
my_set = {1, 2, 2, 3, 4, 4, 5}
print(my_set) # Выведет {1, 2, 3, 4, 5} в любом порядке. Выведет только элементы, которые НЕ ПОВТОРЯЮТСЯ
```

### C помощью функции `set()`

```py
# Создание множества из массива
my_set = set([1, 2, 3, 4, 4, 5, 5])
print(my_set) # Выведет {1, 2, 3, 4, 5} в любом порядке. Выведет только элементы, которые НЕ ПОВТОРЯЮТСЯ
```

### Создание множества из строки

```py
string = input() # Предположим, что мы ввели "Hello, World!"
my_set = set(string)
print(my_set) # Выведет {'H', 'e', ' ', '!', 'o', 'r', 'l', ',', 'd', 'W'} в любом порядке. Выведет только элементы, которые НЕ ПОВТОРЯЮТСЯ
```

### Создание множества из числа

```py
num = int(input()) # Предположим мы ввели число 101598723
my_set = set(str(num)) # Из числа множество создать не удастся, поэтому надо будет превратить его в строку с помощью функции str()
print(my_set) # Выведет {'7', '3', '8', '5', '0', '1', '2', '9'} в любом порядке. Выведет только элементы, которые НЕ ПОВТОРЯЮТСЯ
```

## Полезные команды для множеств

### Добавление элемента во множество

Команда `add()` добавляет во множество один элемент (как в массивах команда `append()`)

```py
s = {1, 2, 3}
s.add(4)
print(s) # Выведет {1, 2, 3, 4} в любом порядке. Выведет только элементы, которые НЕ ПОВТОРЯЮТСЯ
```

### Удаление / получение элементов множества

Команда `remove()` удаляет элемент из множества. Если такого элемента в наборе не окажется, то программа выдаст ошибку.

```py
s = {1, 2, 3, 4, 5}
s.remove(3)
print(s)  # Выведет {1, 2, 4, 5} в любом порядке
s.remove(8) # ОШИБКА - KeyError: 8
```

Команда `discard()` работает, как команда `remove()`, только безопаснее — если элемент не найден, то программа не выдаст ошибки.

```py
s = {1, 2, 3, 4, 5}
s.discard(3)
print(s)  # Выведет {1, 2, 4, 5} в любом порядке
```

Команда `pop()` удаляет или выводит случайный элемент множества.

```py
s = {1, 2, 3, 4, 5}
print(s.pop()) # Выведет один случайный элемент из множества
print(s) # Выведет оставшиеся элементы множества в любом порядке
```

Если же вы хотите полностью очистить множество, используйте команду `clear()`.

```py
s = {1, 2, 3, 4, 5}
s.clear()
print(s)  # Выведет set()
```

## Операции над множествами

### Объединение множеств

В результате **объединения множеств** получается новое множество, которое содержит все **уникальные** элементы **из каждого множества**.

```py
set1 = {1, 2, 3}
set2 = {3, 4, 5}
set3 = set1 | set2
print(set3) # Выедет {1, 2, 3, 4, 5} в любом порядке
```

### Пересечение множеств

В результате **пересечения множеств** получается новое множество, которое **содержит только элементы, которые есть в обоих множествах**.

```py
set1 = {1, 2, 3, 4}
set2 = {3, 4, 5, 6}
set3 = set1 & set2
print(set3)  # Выедет {3, 4} в любом порядке
```

### Разность множеств

В результате **разности множеств** получается новое множество, которое **исключает из все элементы из первого множества, которые есть во втором множестве**.

```py
set1 = {"apple", "banana", "cherry"}
set2 = {"banana", "orange", "mango"}
set3 = set1 - set2
print(set3) # Выведет {'cherry', 'apple'} в любом порядке
```

### Сравнение множеств

Сравнивать множества можно с помощью операторов `==` (равенство) и `!=` (неравенство). Первый проверяет, есть ли в множествах **общие объекты**, а второй — есть ли **различающиеся**.

```py
set1 = {1,2,3,4}
set2 = {4,3,2,1}
set3 = {1,2,3,5}

if (set1 == set2): # Множества set1 и set2 состоят из одинаковых элементов, поэтому выведет "Множества одинаковые"
    print("Множества одинаковые")
else:
    print("Множества разные")
    
if (set1 == set3): # Множества set1 и set3 состоят из разных элементов, поэтому выведет "Множества разные"
    print("Множества одинаковые")
else:
    print("Множества разные")
```

### Как проверить есть ли элемент в множестве

```py
my_set = {1, 2, 3, 4, 5}
a = 4
b = 6

if (a in my_set): # a = 4 есть в множестве, поэтому выведет "Есть"
    print("Есть")
else:
    print("Нет")

if (b in my_set): # b = 6 нет в множестве, поэтому выведет "Нет"
    print("Есть")
else:
    print("Нет") 
```

### Как проверить, является ли одно множество подмножеством другого

То есть все элементы из первого множества есть во втором

```py
set1 = {1, 2, 3}
set2 = {1, 2, 3, 4, 5}
set3 = {1, 2, 3}
set4 = {1, 2, 5, 8}

if (set1.issubset(set2)): # Проверяет все ли элементы из множества set1 есть в множестве set2. В нашем случае да, поэтому выведет "Да"
    print("Да")
else:
    print("Нет")
    
    
if (set3.issubset(set4)): # Проверяет все ли элементы из множества set3 есть в множестве set4. В нашем случае да, поэтому выведет "Нет"
    print("Да")
else:
    print("Нет")
```

# Словари в Python

Словарь в языках программирования — это что-то вроде телефонной книги, где под каждым номером скрывается какой-то человек. При этом ключи в словарях уникальны, а значения могут повторяться.

![image](https://github.com/user-attachments/assets/40bb9807-87ce-404b-af14-68180888803e)

![image](https://github.com/user-attachments/assets/f48283e2-102e-45fa-99b5-ca099cc41789)

## Создание словаря

```py
books = {
  'Гарри Поттер и философский камень' : 'Джоан Роулинг', # Первое значение - ключ, второе - значение 
  'Убить пересмешника' : 'Харпер Ли'
}

print(books['Гарри Поттер и философский камень']) # Выведет значение под ключом 'Гарри Поттер и философский камень', то есть выведет 'Джоан Роулинг'
```

## Как добавить новый элемент в словарь

```py
dict = {
  'Гарри Поттер и философский камень' : 'Джоан Роулинг',
  'Убить пересмешника' : 'Харпер Ли'
}

dict['Преступление и наказание'] = 'Фёдор Достоевский' # Добавляет в словарь dict новое значение 'Фёдор Достоевский' под ключом 'Преступление и наказание'

print(dict) # Выведет {'Гарри Поттер и философский камень': 'Джоан Роулинг', 'Убить пересмешника': 'Харпер Ли', 'Преступление и наказание': 'Фёдор Достоевский'}
```

## Как удалить элемент из словаря 

```py
dict = {
  'Гарри Поттер и философский камень' : 'Джоан Роулинг',
  'Убить пересмешника' : 'Харпер Ли',
}

del dict['Убить пересмешника'] # Удаляет из словаря значение с ключом 'Убить пересмешника'

print(dict) # Выведет {'Гарри Поттер и философский камень': 'Джоан Роулинг'}
```

## Команда `get()`

```py
dict = {
  'Гарри Поттер и философский камень' : 'Джоан Роулинг',
  'Убить пересмешника' : 'Харпер Ли'
}

harryPotterAuthor = dict.get('Гарри Поттер и философский камень') # Возвращает значение из словаря по ключу 'Гарри Поттер и философский камень'

print(harryPotterAuthor) # Выведет 'Джоан Роулинг'
```

## Команда `values()`

```py
dict = {
  'Гарри Поттер и философский камень' : 'Джоан Роулинг',
  'Убить пересмешника' : 'Харпер Ли'
}

dictValues = dict.values() # Возвращает все значения из словаря, но без ключей

print(dictValues) # Выведет ['Джоан Роулинг', 'Харпер Ли'] 
```

# Работа с файлами

## Чтение из файла:

Открывает в переменную `f` файл с именем 'text.txt' для чтения из него `'r'` (`read` = читать). Вместо `input()` используем `read()`.

```py
f = open('text.txt', 'r') # Открывает файл в переменную f (может быть любая другая буква) для чтения

# Помещаем все содержимое файла text.txt в массив a, где каждый элемент - это строка файла
a = f.read().split("\n")
print(a) # Любое действие со строками файла, например, вывожу все


# ИЛИ построчно читаем линии 
for line in f:
    print(line) # Любое действие с ними, например, вывести

f.close() # Закрывает файл
```

## Запись в файл:

Открывает в переменную `f` файл с именем 'text.txt' для записи в него `'w'` (`write` = писать). Вместо `print()` используем `write()`.

```py
f = open('text.txt', 'w') # Открывает файл в переменную f (может быть любая другая буква) для записи в него

f.write("Что угодно") # Записвает в файл строку "Что угодно" (может быть любое сообщение)

f.close() # Закрывает файл
```

# Функции

Функция в python - объект, принимающий аргументы и возвращающий значение. Переменных можно вводить сколько угодно (хоть 0, хоть 100), называть функцию можно как угодно и делать функция будет тоже что угодно, что вы захотите (главное написать код).

```py
def название_функции(переменная_1, переменная_2, ...):
    действие
```

Пример:

```py
def add(x, y):
    return x + y

print(add(1, 10)) # Выведет 11
```
